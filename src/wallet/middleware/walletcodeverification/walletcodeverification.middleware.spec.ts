import { WalletcodeverificationMiddleware } from './walletcodeverification.middleware';

describe('WalletcodeverificationMiddleware', () => {
  it('should be defined', () => {
    expect(new WalletcodeverificationMiddleware()).toBeDefined();
  });
});
